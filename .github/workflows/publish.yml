---
name: Quarto Publish

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: paddyroddy/.github/actions/github-pages/quarto@5aa2f0fea5f954ca1f8146d44e9c618c99eb64ca # v0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
